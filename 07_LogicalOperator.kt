fun main(args: Array<String>) {
    var isCold = true
    var isDry = false

    // && (AND Operator)
    var result = isCold && isDry
    println(result)

    // || (OR Operator)
    result = isCold || isDry
    println(result)
}
